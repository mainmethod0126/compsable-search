# [T-158] 구현한다: 결정적 샘플 생성 유틸과 DemoService 연동

## 메타
- ID: `T-158`
- 소속 UserStory: `[US-040] 프로파일 정의와 결정적 샘플 생성기 구축` (`../userstory.md`)
- 소속 Feature: `[F-13] 결정적 지역 샘플 프로파일과 대량 검증 워크플로우` (`../../feature.md`)
- 소속 Epic: `[E-05] 대량 지역 샘플 확장과 검증 가능성 확보` (`../../../epic.md`)
- 유형: `프론트엔드`
- 담당 역할: `프론트엔드 엔지니어`
- 우선순위: `P0`
- 난이도: `High`
- GitHub Issue: `TBD`

## 작업 내용
- 목표: 프로파일 기반 결정적 데이터 생성과 데모 데이터 공급 경로를 구현한다.
- 구현 항목: 프로파일 입력(`small`, `medium`, `large`)을 받아 동일 결과를 반환하는 생성 유틸을 구현한다. / 상위-하위 지역 코드 규칙을 생성 로직에 반영한다. / `src/DemoService.ts`를 프로파일 기반 생성기로 교체 또는 확장한다.
- 선행조건: [T-157] 정의한다: 프로파일 규모와 생성 규칙 수용 기준 완료

## 검증과 완료 조건
- 검증 방법(테스트/리뷰/지표): 코드 리뷰와 로컬 실행으로 프로파일 전환 시 데이터 일관성을 확인한다.
- 산출물: 생성 유틸 코드, `DemoService` 연동 코드
- 완료 정의(DoD): 동일 프로파일 호출 결과가 재현 가능하고 `DemoService`에서 프로파일별 데이터를 안정적으로 제공한다.
