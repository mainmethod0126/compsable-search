# [T-178] 구현한다: 지역 검색 인덱스와 필터 모델

## 메타
- ID: `T-178`
- 소속 UserStory: `[US-045] 지역 검색 인덱스와 매칭 정책 구축` (`../userstory.md`)
- 소속 Feature: `[F-15] 지역 검색 모델과 미리보기 선택 연동 구현` (`../../feature.md`)
- 소속 Epic: `[E-06] 지역 검색 미리보기 선택 경험 구현` (`../../../epic.md`)
- 유형: `프론트엔드`
- 단계: `기능-구현`
- 담당 역할: `프론트엔드 엔지니어`
- 우선순위: `P0`
- 난이도: `High`
- GitHub Issue: `TBD`

## 작업 내용
- 목표: 지역 트리 평탄화 인덱스와 부분 일치 필터/경로 라벨 생성 로직을 순수 함수로 구현한다.
- 구현 항목: `regionSearchModel.ts`(신규) 구현으로 인덱스 생성 함수를 제공한다. / 부분 일치 필터와 결과 제한 함수를 구현한다. / 미리보기 선택 시 조건 매핑 헬퍼를 구현한다.
- 선행조건: `[T-176] 확정한다: 지역 검색 컴포넌트 디자인 핸드오프`, `[T-177] 정의한다: 지역 검색 매칭 정책과 경계 조건`이 완료되어야 한다.

## 검증과 완료 조건
- 검증 방법(테스트/리뷰/지표): 코드 리뷰와 단위 테스트 실행으로 정책 반영 여부를 확인한다.
- 산출물: 검색 모델 코드, 매핑 헬퍼 코드, 정책 반영 주석
- 완료 정의(DoD): 검색 모델이 UI 컴포넌트 의존 없이 독립 테스트 가능하며 정책 문서와 일치한다.

