# [T-182] 검증한다: 지역 검색 플로우 통합/회귀 테스트

## 메타
- ID: `T-182`
- 소속 UserStory: `[US-046] 미리보기 UI와 선택 조건 연동` (`../userstory.md`)
- 소속 Feature: `[F-15] 지역 검색 모델과 미리보기 선택 연동 구현` (`../../feature.md`)
- 소속 Epic: `[E-06] 지역 검색 미리보기 선택 경험 구현` (`../../../epic.md`)
- 유형: `테스트`
- 단계: `검증`
- 담당 역할: `QA 엔지니어`
- 우선순위: `P0`
- 난이도: `Medium`
- GitHub Issue: `TBD`

## 작업 내용
- 목표: 신규 검색 기능과 기존 지역/키워드 플로우의 회귀 위험을 통합 테스트로 차단한다.
- 구현 항목: `ComposableSearch.test.tsx`에 검색 입력/미리보기/클릭 선택 시나리오를 추가한다. / 검색 컴포넌트의 DOM 배치 계약(`cs-selector-area` 하위, `cs-detailed-area` 외부) 검증 시나리오를 추가한다. / 필요 시 `callbackContract.test.tsx`에 콜백 계약 회귀 시나리오를 보강한다. / 기존 지역 선택·키워드 입력 시나리오 회귀를 재검증한다. / large 프로파일 수동 스모크 결과를 기록한다.
- 선행조건: `[T-181] 연동한다: 미리보기 선택과 조건 정책/콜백 파이프라인`이 완료되어야 한다.

## 검증과 완료 조건
- 검증 방법(테스트/리뷰/지표): `npm run test`, 필요 시 `npm run test:large` 실행 결과를 확인한다.
- 산출물: 통합/회귀 테스트 코드, 테스트 실행 로그, 수동 스모크 기록
- 완료 정의(DoD): 신규 시나리오와 기존 회귀 시나리오가 모두 통과하고 실패 원인 없는 상태로 공유된다.

