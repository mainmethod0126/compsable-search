# [T-171] 검증한다: 스크롤 도입 후 삭제/동기화 회귀와 스타일 계약

## 메타
- ID: `T-171`
- 소속 UserStory: `[US-043] 선택 조건 영역 스크롤과 레이아웃 보호` (`../userstory.md`)
- 소속 Feature: `[F-03] 선택 조건 칩/삭제/중복 방지 처리` (`../../feature.md`)
- 소속 Epic: `[E-01] 코어 지역 선택 경험 MVP 완성` (`../../../epic.md`)
- 유형: `테스트`
- 담당 역할: `QA 엔지니어, 프론트엔드 엔지니어`
- 우선순위: `P0`
- 난이도: `Medium`
- GitHub Issue: `TBD`

## 작업 내용
- 목표: 스크롤 컨테이너 도입 이후에도 기능 계약과 스타일 계약이 회귀 없이 유지됨을 증명한다.
- 구현 항목: `ComposableSearch.test.tsx`에 다수 칩 상태 삭제/전체 삭제/동기화 시나리오를 보강한다. / `styleIsolation.test.ts`에 선택 조건 영역 `max-height`, `overflow-y` 계약 검증을 추가한다. / 모바일/데스크톱 수동 점검 결과를 테스트 증적으로 기록한다.
- 선행조건: [T-170] 구현한다: 선택 조건 영역 스크롤 viewport와 레이아웃 보호 규칙 완료

## 검증과 완료 조건
- 검증 방법(테스트/리뷰/지표): `npm test` 실행 결과와 리뷰 체크리스트로 정상/예외 흐름 회귀 여부를 확인한다.
- 산출물: 테스트 코드, 테스트 실행 로그, 수동 검증 결과 기록
- 완료 정의(DoD): 자동 테스트가 CI 기준으로 통과하고 칩 대량 상태 핵심 시나리오 결함이 재현되지 않는다.
