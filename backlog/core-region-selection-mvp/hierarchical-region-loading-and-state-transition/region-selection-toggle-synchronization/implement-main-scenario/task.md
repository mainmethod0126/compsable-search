# [T-022] 구현한다: 지역 선택 체크 토글 동기화 핵심 시나리오

## 메타
- ID: `T-022`
- 소속 UserStory: `[US-006] 지역 선택 체크 토글 동기화` (`../userstory.md`)
- 소속 Feature: `[F-02] 계층형 지역 로딩과 선택 상태 전이` (`../../feature.md`)
- 소속 Epic: `[E-01] 코어 지역 선택 경험 MVP 완성` (`../../../epic.md`)
- 유형: `프론트엔드`
- 담당 역할: `프론트엔드 엔지니어`
- 우선순위: `P0`
- 난이도: `High`
- GitHub Issue: `TBD`

## 작업 내용
- 목표: 시/군/구 컬럼 `시/도 전체` 직접 체크 시나리오와 읍/면/동 토글 동기화를 동작 가능하게 만든다.
- 구현 항목: `시/도 전체` 체크박스 토글을 시/군/구 컬럼에서 처리하고 선택 상태에 즉시 반영한다. / 읍/면/동 컬럼의 `시/도 전체` 중복 노출 경로를 제거한다. / `부산광역시 해운대구` 선선택 후 `부산광역시 전체` 체크 시 기존 `해운대구` 조건이 즉시 해제되도록 상호 배타 정책(FR-011/FR-019/FR-020)을 적용한다. / 공개 API 계약 호환성을 유지한다.
- 선행조건: [T-021] 정의한다: 지역 선택 체크 토글 동기화 범위와 수용 기준 완료

## 검증과 완료 조건
- 검증 방법(테스트/리뷰/지표): 로컬 검증과 코드 리뷰에서 정상/예외 흐름 동작을 확인한다.
- 산출물: 코드 변경셋, 관련 타입/문서 업데이트
- 완료 정의(DoD): 수용 기준의 정상/예외 항목이 구현되고 리뷰 승인된다.

