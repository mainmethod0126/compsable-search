# [T-023] 검증한다: 지역 선택 체크 토글 동기화 회귀와 실패 시나리오

## 메타
- ID: `T-023`
- 소속 UserStory: `[US-006] 지역 선택 체크 토글 동기화` (`../userstory.md`)
- 소속 Feature: `[F-02] 계층형 지역 로딩과 선택 상태 전이` (`../../feature.md`)
- 소속 Epic: `[E-01] 코어 지역 선택 경험 MVP 완성` (`../../../epic.md`)
- 유형: `테스트`
- 담당 역할: `QA 엔지니어`
- 우선순위: `P0`
- 난이도: `Medium`
- GitHub Issue: `TBD`

## 작업 내용
- 목표: 시/군/구 `시/도 전체` 직접 체크 도입이 기존 기능 회귀 없이 동작함을 증명한다.
- 구현 항목: `서울특별시 전체`를 시/군/구에서 직접 체크/해제하는 케이스와 `부산광역시 해운대구` 선택 후 `부산광역시 전체` 체크 시 기존 `해운대구` 자동 해제 케이스를 추가한다. / 읍/면/동 컬럼에 동일 `시/도 전체` 항목이 중복 노출되지 않음을 검증한다. / FR-011·FR-019·FR-020 경계/실패 시나리오와 회귀 결과를 PR/문서에 기록한다.
- 선행조건: [T-022] 구현한다: 지역 선택 체크 토글 동기화 핵심 시나리오 완료

## 검증과 완료 조건
- 검증 방법(테스트/리뷰/지표): 테스트 스위트 실행 결과와 리뷰 체크리스트를 기준으로 검증한다.
- 산출물: 자동화 테스트 코드, 실행 로그, 회귀 검증 기록
- 완료 정의(DoD): 관련 테스트가 CI 기준을 통과하고 실패 재현 케이스가 커버된다.

